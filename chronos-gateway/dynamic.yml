http:
  routers:

    chronos-sds:
      entryPoints:
        - web
      rule: "PathPrefix(`/api/schema`)"
      service: chronos-sds
      #      middlewares:
      #        - strip-api-prefix
      priority: 100

    chronos-ws:
      entryPoints:
        - web
      rule: "PathPrefix(`/api/wiki`)"
      service: chronos-ws
      priority: 99

    chronos-uis:
      entryPoints:
        - web
      rule: "PathPrefix(`/api/ui`)"
      service: chronos-uis
      priority: 98

    chronos-hds:
      entryPoints:
        - web
      rule: "PathPrefix(`/api`)"
      service: chronos-hds
      #      middlewares:
      #        - strip-api-prefix
      priority: 80

    chronos-ui:
      entryPoints:
        - web
      rule: "PathPrefix(`/`)"
      service: chronos-ui
      priority: 1

  #  middlewares:
  #    strip-api-prefix:
  #      stripPrefix:
  #        prefixes:
  #          - "/api"

  services:

    chronos-sds:
      loadBalancer:
        passHostHeader: true
        servers:
          - url: "http://chronos-sds:8090"

    chronos-hds:
      loadBalancer:
        passHostHeader: true
        servers:
          - url: "http://chronos-hds:8010"

    chronos-uis:
      loadBalancer:
        passHostHeader: true
        servers:
          - url: "http://chronos-uis:8040"

    chronos-ws:
      loadBalancer:
        passHostHeader: true
        servers:
          - url: "http://chronos-ws:8050"

    chronos-ui:
      loadBalancer:
        passHostHeader: true
        servers:
          - url: "http://chronos-ui:4200"
