import { ElementAttributePipe } from './element-attribute.pipe';

describe('ElementAttributePipe', () => {
  it('create an instance', () => {
    const pipe = new ElementAttributePipe();
    expect(pipe).toBeTruthy();
  });
});
